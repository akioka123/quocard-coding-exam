services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=book_management'
      - 'POSTGRES_PASSWORD=Api!!1234'
      - 'POSTGRES_USER=api'
    ports:
      - '5432:5432'
